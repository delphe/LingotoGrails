package com.lingoto

class HomeController {
	//IE message "this page uses fonts that need to be temporarily installed." may occur on all pages.
	//May want to troubleshoot if it becomes an issue. Here's a workaround for the user:
	//http://answers.microsoft.com/en-us/ie/forum/ie8-windows_other/temporary-fonts-when-visiting-specific-sites-im/80cc2fa7-69ad-4329-a1e3-44c3821a21ae
	
	//TODO: Change Contact Us & flag drop down to list to show languages with each country flag to the right of it... English, Español, Magyar, Deutsche.
			//This can be used when registering as well to select the lang/dialect with the associated flags
	
	//TODO: Phase 2 - Create a DB table that shows all country codes belonging to each language. Display all flags for that language in _language.gsp.
	
    def index = {}

}